name: Deploy to HuggingFace Spaces

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Push to HuggingFace Space
        env:
          HF_TOKEN: ${{ secrets.HF_TOKENG }}
        run: |
          git config --global user.email "dharnilotpal31@gmail.com"
          git config --global user.name "nilotpaldhar2004"

          git clone https://nilotpaldhar2004:${HF_TOKEN}@huggingface.co/spaces/nilotpaldhar2004/dermsight-pro hf_space

          cp main.py hf_space/
          cp index.html hf_space/
          cp requirements.txt hf_space/
          cp Dockerfile hf_space/

          # Model files — only copy if they exist in repo
          # (they may be gitignored and uploaded manually to HF Space)
          [ -f classes.npy ] && cp classes.npy hf_space/
          [ -f skin_lesion_resnet50_best.pth ] && cp skin_lesion_resnet50_best.pth hf_space/

          cd hf_space
          git add .
          git diff --cached --quiet || git commit -m "Auto deploy from GitHub"
          git remote set-url origin https://nilotpaldhar2004:${HF_TOKEN}@huggingface.co/spaces/nilotpaldhar2004/dermsight-pro
          git push origin main
